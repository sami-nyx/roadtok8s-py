name: Hello World
on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Hello Worldenv:
          MY_SECRET: ${{ secrets.SECRET_VAR }}
        run: echo "Hello World secret: $MY_SECRET:"
